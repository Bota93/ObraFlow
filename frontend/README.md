# ObraFlow Frontend

This folder is reserved for the future ObraFlow frontend application.

The repository has already been prepared as a monorepo, so the frontend can be implemented here without mixing Node-based tooling with the .NET backend workspace in `../backend`.
