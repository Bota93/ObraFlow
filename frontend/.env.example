# Production target for Vercel:
VITE_API_BASE_URL=https://api-obraflow.adrianalcaraz.es
# Local development can override this with:
# - http://localhost:5250 when the backend runs with dotnet run
# - http://localhost:5000 when the backend runs through backend/docker-compose.yml
